<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cheng.lkc12306">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".my.ContactActivity" />
        <activity android:name=".my.ContactEditActivity" />
        <activity android:name=".my.ContactAddActivity" />
        <activity android:name=".my.AccountActivity" />
        <activity android:name=".my.PassWordActivity" />
        <activity android:name=".stationlist.StationListActivity" />
        <activity
            android:name=".login.SplashActivity"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".ticket.TicketResultStep1Activity" />
        <activity android:name=".ticket.TicketResultStep2Activity"></activity>
    </application>

</manifest>